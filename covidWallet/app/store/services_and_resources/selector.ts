import { RootState } from '..';

// export const selectResources = (s: RootState) => s.serviceandresource.resources;

// export const selectResourcesLoading = (s: RootState) => s.serviceandresource.loading;

// export const selectResourcesError = (s: RootState) => s.serviceandresource.error;

export const selectResourcesState = (s: RootState) => s.serviceandresource;
