import { StyleSheet } from 'react-native';
import { AppColors, BACKGROUND_COLOR } from './Colors';

export const themeStyles = StyleSheet.create({
  mainContainer: {
    backgroundColor: AppColors.BACKGROUND,
    flex: 1,
    padding: 10,
  },
});
